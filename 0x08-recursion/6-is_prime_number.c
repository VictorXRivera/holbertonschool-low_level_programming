#include "holberton.h"

/**
  * is_prime_number - function to find prime number
  * @n: Integer parameter
  * Return: 1
  */
int is_prime_number(int n)
{
	if (n % 2 > 1)
		return (1);

	return (0);
}
