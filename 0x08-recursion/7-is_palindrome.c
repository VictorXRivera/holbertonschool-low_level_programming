#include "holberton.h"

/**
  * is_palindrome - function to check if string a palindrome
  * @s: pointer to a string
  * Return: 1 if palindrome, 0 if not
  */
int is_palindrome(char *s)
{
	return (1);
}
